apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-deploy
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
      namespace: dev
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
      - name: redis-container
        image: redis:latest
        resources:
          requests:
            memory: "128Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 6379
